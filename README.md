# Simple Web App

A dummy Python Flask app used for testing CI/CD, DevOps scripts, and automation pipelines.

## Features
- REST endpoints (`/health`, `/hello/<name>`, `/`)
- Dockerized app
- Unit tests with pytest
- Lightweight and easy to deploy

## Run locally
```bash
pip install -r requirements.txt
python app/main.py

